---
# This collection is initialized by https://github.com/ansible/ansible-creator 26.4.3

# See https://docs.ansible.com/ansible/latest/dev_guide/collections_galaxy_meta.html

namespace: "david_igou"
name: "rustfs"
version: 2.0.0
readme: README.md
authors:
  - David Igou <igou.david@gmail.com>

description: >-
  Declarative in-server state management for RustFS object-storage servers
  via native modules speaking the S3 and admin APIs directly: buckets,
  versioning, lifecycle, quota, IAM policies/users/groups/service accounts,
  policy attachments, and credential liveness verification, with check mode
  as drift detection.
license_file: LICENSE

tags:
  - storage
  - s3
  - rustfs
  - infrastructure
dependencies: {}

repository: https://github.com/david-igou/ansible-collection-rustfs
documentation: https://github.com/david-igou/ansible-collection-rustfs/blob/main/README.md
homepage: https://github.com/david-igou/ansible-collection-rustfs
issues: https://github.com/david-igou/ansible-collection-rustfs/issues

# A list of file glob-like patterns used to filter any files or directories that should not be included in the build
# artifact. A pattern is matched from the relative path of the file or directory of the collection directory. This
# uses 'fnmatch' to match the files or directories. Some directories and files like 'galaxy.yml', '*.pyc', '*.retry',
# and '.git' are always filtered. Mutually exclusive with 'manifest'
build_ignore:
  - .gitignore
  - .ansible
  - david_igou-rustfs-*.tar.gz
  - importer_result.json
  - tests
  - extensions
  - Makefile
  - ansible.cfg
  - .github
  - .config
  - .isort.cfg
  - .pre-commit-config.yaml
  - .prettierignore
  - pyproject.toml
  - tox-ansible.ini
  - requirements.txt
  - test-requirements.txt
  - AGENTS.md
  - renovate.json
# A dict controlling use of manifest directives used in building the collection artifact. The key 'directives' is a
# list of MANIFEST.in style
# L(directives,https://packaging.python.org/en/latest/guides/using-manifest-in/#manifest-in-commands). The key
# 'omit_default_directives' is a boolean that controls whether the default directives are used. Mutually exclusive
# with 'build_ignore'
# manifest: null
